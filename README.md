# 12WeekDev - 12 周学习计划追踪器

一个基于 Vue 3 + TypeScript + Supabase 的学习管理系统，帮助追踪前端进阶学习计划。集成 AI 智能规划，支持个性化学习路径生成。

## ✨ 功能特性

- 🤖 **AI 智能规划** (New) - 输入角色和 JD，一键生成 12 周详细学习大纲并入库
- ⚙️ **数据私有化** (New) - 支持在设置页配置自己的 Supabase 和 AI Key，数据掌握在自己手中
- 📋 **每日任务** - 学习任务清单，支持完成勾选
- 📝 **每日复盘** - 记录学习心得、遇到的问题和明日计划
- 📒 **笔记链接** - 关联外部笔记（语雀/Notion/飞书）
- 📊 **进度统计** - 可视化图表展示学习进度
- 📚 **学习资源** - 精选资源推荐
- ✨ **额外学习** - 记录计划外的学习内容
- 🏆 **里程碑** - 关键节点提醒和倒计时

## 📅 更新日志

### v1.2 系统动态化与业务闭环 (2025-12-19)
本次更新着重打通了 AI 规划到资源管理的业务闭环，并实现了全系统的动态化，使其不再局限于“12周”或“LeetCode”特定场景。

#### 1. 🔗 业务连贯性升级
- **AI 智能资源推荐**：AI 规划器现在会自动为每个任务推荐 1-2 个高质量学习链接，并自动存入数据库。
- **资源管理闭环**：新增 `task_resources` 表，打通了 `AI 生成 -> 数据库存储 -> 前端展示` 的完整链路。
- **每日任务联动**：在任务详情卡片中直接展示关联的学习资源，支持手动添加/删除资源链接。

#### 2. 🧩 系统动态化改造
- **自适应学习大纲** (`Roadmap`)：
  - 移除了硬编码的阶段划分，支持基于里程碑（Milestone）的**自适应阶段生成**。
  - 标签系统升级为**动态推断**，根据每周任务类型自动生成“专项学习”、“项目实战”等标签，不再强制显示“算法基础”。
- **通用进度统计** (`Progress`)：
  - 将“LeetCode 刷题统计”泛化为“算法/代码练习统计”，仅在计划包含相关任务时显示。
  - 图表统计类别根据实际任务类型动态生成。

#### 3. 🛠 数据库与体验优化
- **数据库迁移**：移除了任务类型的强约束，支持 `project`、`other` 等任意类型任务写入。
- **UI 细节**：修复了设置页长文本输入体验，优化了密码框显示逻辑。

---

### v1.1 更新日志 (2025-12-19)

本次更新重点增强了系统的灵活性和智能化体验：

### 1. 🤖 新增 AI 学习规划生成器
- **功能入口**：侧边栏 -> `AI 规划`
- **使用方式**：
  1. 填写你的身份（如：大三学生）、工作年限。
  2. 粘贴心动岗位的 JD (招聘要求)。
  3. 点击生成，AI 将为你量身定制 12 周学习路线（包含里程碑和每日任务）。
  4. 预览无误后，点击 **"初始化数据库"**，直接将计划写入你的 Supabase 数据库。
- **手动模式**：如果你不想配置 API Key，可以查看项目根目录下的 `PROMPTS.md`，复制提示词到 Trae/Cursor 对话框手动生成 JSON。

### 2. ⚙️ 系统设置与数据私有化
- **功能入口**：侧边栏底部 -> `系统设置`
- **灵活配置**：
  - **Supabase 配置**：不再强制依赖 `.env` 文件。你可以在设置页直接输入 Supabase URL 和 Key，系统会优先使用本地存储的配置。
  - **AI 模型配置**：支持配置 DeepSeek 或 OpenAI 的 API Key 和 Base URL。
  - **学习周期**：支持自定义学习开始日期，系统会自动重算周次。

### 3. 🛠 体验优化
- 修复了 Vetur/Volar 在 TypeScript 导入时的模块解析报错。
- 增加了 cSpell 词典配置，消除 `supabase`、`pinia` 等技术名词的拼写误报。
- 优化了路由结构，新增 `/plan` 和 `/settings` 页面。

---

## 🛠 技术栈

- **前端**: Vue 3 + TypeScript + Vite
- **AI 集成**: LangChain (OpenAI/DeepSeek)
- **样式**: TailwindCSS
- **状态管理**: Pinia (持久化存储)
- **图表**: ECharts
- **后端**: Supabase (PostgreSQL + RESTful API)

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone <your-repo-url>
cd 12WeekDev-Template
```

### 2. 安装依赖

```bash
pnpm install
# 或 npm install
```

### 3. 启动项目

```bash
pnpm run dev
```

### 4. 系统配置（推荐）

项目启动后，访问 http://localhost:5173，点击左下角的 **"系统设置"**：
1. **配置数据库**：填入你的 Supabase URL 和 Key（无需修改代码或环境变量）。
2. **配置 AI**：填入 DeepSeek API Key 以启用智能规划功能。
3. **初始化表结构**：在 Supabase SQL Editor 执行 `supabase/migrations/001_init.sql`。

*(可选) 传统方式：复制 `.env.example` 为 `.env` 并填入环境变量。*

## 📁 项目结构

```
src/
├── views/           # 页面组件 (新增 PlanGenerator.vue, Settings.vue)
├── stores/          # Pinia 状态管理 (新增 settingsStore.ts)
├── lib/             # Supabase 客户端 (支持动态配置)
├── types/           # TypeScript 类型
└── utils/           # 工具函数
PROMPTS.md           # AI 提示词模板 (手动生成规划用)
```

## 📝 License

MIT
